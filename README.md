# GSM-tool
The goal is to build an open-source tool that allows anyone to launch a dedicated game server (like Project Zomboid or Minecraft) using Docker with just one command, while a background script automatically handles game saves and backups so players never lose their progress.
